---
import Base from '@/layouts/Base';
import MainNav from '@/components/MainNav';
import MainView from '@/components/MainView';

const meta = {
  title: '404 Not Found',
  description: 'ご指定のページが見つかりませんでした。',
};
---

<Base meta={meta} gridType={'box'}>
  <MainView />
  <MainNav />
</Base>
